AddMul {
  Expr = Add

  Add
    = Add "+" Mul  -- plus
    | Mul

  Mul
    = Mul "*" Pri  -- times
    | Pri

  Pri
    = number
    | "(" Expr ")"  -- paren

  number = digit+   // используем встроенный digit
}
